#!/usr/bin/groovy

def project = "expenseManager"
def version = -1
def imageName = "expense-manager"

node {
    stage('Delete Workspace') {
        sh 'rm -rf *'
        sh 'rm -rf .git'
        sh 'rm -f .gitignore'
    }

}